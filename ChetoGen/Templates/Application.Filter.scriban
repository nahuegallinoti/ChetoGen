namespace {{BASE_NS}}.Application.Models.App;

/// <summary>
/// Filtro + paginación + orden (server-side) para {{ENTITY_PLURAL}}.
/// Hereda Page / PageSize / SortBy / SortDesc de PagedQuery ({{BASE_NS}}.Application.Models).
/// </summary>
public sealed class {{ENTITY}}Filter : PagedQuery
{
{{PROPS_FILTER_DTO}}
}
